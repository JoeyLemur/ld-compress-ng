#version 450

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer SmokeData {
    uint values[];
} data_buffer;

void main()
{
    const uint index = gl_GlobalInvocationID.x;
    data_buffer.values[index] = data_buffer.values[index] * 3u + 7u;
}
